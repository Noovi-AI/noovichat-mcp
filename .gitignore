# Dependencies
node_modules/
jspm_packages/
.pnpm-store/

# Build artifacts
dist/
build/
out/
*.tsbuildinfo

# Coverage / test artifacts
coverage/
*.lcov
.nyc_output/

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment & secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
*.cer
*.crt
*.p12
*.pfx
secrets.*
credentials.*
*.private
*-key.json
service-account*.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
!.vscode/extensions.json
.idea/
*.iml
.fleet/
.zed/

# Lock files — keep pnpm-lock.yaml committed (required by CI and Renovate).
# Ignore the others so devs accidentally running npm/yarn don't litter the repo.
package-lock.json
yarn.lock

# Local Claude Code / agent transcripts
.claude/sessions/
.claude/cache/
.claude/local/
.claude.local.json

# Internal scratch / research artifacts (must never go public)
.scratch/
.notes/
TODO.local.md
NOTES.local.md

# Temp
tmp/
temp/
.tmp/

# MCP host configs that might embed real tokens during local dev
claude_desktop_config.json
mcp-config.local.json
